package com.example.landscape_mode

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MainActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        /*
        * Landscape is very easy to createable.
        * Open design of main.xml.
        * Now go to landscape option and click on more.
        * Select the orientation and slct landscape mode.
        * Now just design it according to layout.
        * Nothing else to do it will automatic work.
        * in res file landscape main.xml will be automatically created.*/

    }
}
